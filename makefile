# PREFIX=/usr/local

rad2deg:
	cc -o rad2deg rad2deg.c

# install: cdelta
# 	mkdir -p ${PREFIX}/bin
# 	install cdelta ${PREFIX}/bin/cdelta

# uninstall:
# 	rm -rf ${PREFIX}/bin/cdelta

# clean:
# 	rm -rf cdelta
